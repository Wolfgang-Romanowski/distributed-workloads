name: Comprehensive Snyk Security Analysis

on:
  push:
    branches: [ main ]
    paths:
      - 'images/runtime/training/*/Dockerfile'
      - 'images/runtime/training/**/requirements.txt'
      - 'images/runtime/training/**/package.json'
      - 'images/runtime/training/**/Pipfile'
  pull_request:
    paths:
      - 'images/runtime/training/*/Dockerfile'
      - 'images/runtime/training/**/requirements.txt'
      - 'images/runtime/training/**/package.json'
      - 'images/runtime/training/**/Pipfile'
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:
    inputs:
      target_dir:
        description: 'Specific directory to scan (leave empty for all)'
        required: false
        default: ''
        type: string
      scan_type:
        description: 'Scan type'
        required: false
        default: 'comprehensive'
        type: choice
        options:
          - 'comprehensive'
          - 'container-only'
          - 'code-only'
          - 'dependencies-only'

jobs:
  snyk-comprehensive-scan:
    name: Comprehensive Snyk Security Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    strategy:
      fail-fast: false
      matrix:
        scan-type: 
          - container
          - code
          - iac
          - dependencies
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # Fetch full history for better analysis
          fetch-depth: 0

      - name: Setup Snyk
        uses: snyk/actions/setup@master
        with:
          snyk-version: latest

      - name: Authenticate Snyk
        run: |
          if [ -z "${{ secrets.SNYK_TOKEN }}" ]; then
            echo "::error::SNYK_TOKEN secret is not configured"
            exit 1
          fi
          snyk auth "${{ secrets.SNYK_TOKEN }}"
          
          # Verify authentication and show account info
          snyk config get api
          echo "Snyk organization: $(snyk config get org || echo 'default')"

      - name: Setup Analysis Environment
        run: |
          mkdir -p results/{container,code,iac,dependencies,reports}
          
          # Install additional tools for comprehensive analysis
          sudo apt-get update
          sudo apt-get install -y jq yq curl

      - name: Discover and Analyze Components
        id: discovery
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
          TARGET_DIR: ${{ github.event.inputs.target_dir }}
          SCAN_TYPE: ${{ github.event.inputs.scan_type }}
        run: |
          set -e
          
          # Target directories discovery
          TRAINING_DIR="images/runtime/training"
          if [ -n "$TARGET_DIR" ]; then
            if [ -d "$TRAINING_DIR/$TARGET_DIR" ]; then
              TARGET_DIRS=("$TARGET_DIR")
            else
              echo "::error::Directory $TARGET_DIR not found"
              exit 1
            fi
          else
            TARGET_DIRS=($(find "$TRAINING_DIR" -name "Dockerfile" -exec dirname {} \; | xargs -I {} basename {}))
          fi
          
          echo "Analyzing directories: ${TARGET_DIRS[*]}"
          echo "Scan type: ${SCAN_TYPE:-comprehensive}"
          
          # Enhanced base image discovery with resolution
          declare -A BASE_IMAGES DOCKERFILE_BASE_MAP DOCKERFILE_CONTEXTS
          
          for DIR in "${TARGET_DIRS[@]}"; do
            DOCKERFILE_PATH="$TRAINING_DIR/$DIR"
            DOCKERFILE="$DOCKERFILE_PATH/Dockerfile"
            
            if [ ! -f "$DOCKERFILE" ]; then
              echo "::warning::Dockerfile not found: $DOCKERFILE"
              continue
            fi
            
            echo "Analyzing $DIR..."
            
            # Store full path for context analysis
            DOCKERFILE_CONTEXTS["$DIR"]="$DOCKERFILE_PATH"
            
            # Enhanced base image resolution
            BASE_IMAGE_RAW=$(grep -m 1 "^FROM" "$DOCKERFILE" | awk '{print $2}' | cut -d' ' -f1)
            
            # Advanced variable resolution
            RESOLVED_BASE_IMAGE="$BASE_IMAGE_RAW"
            
            # Extract variables from directory context
            if [[ "$DIR" =~ py([0-9]+) ]]; then
              PYTHON_VER="${BASH_REMATCH[1]}"
              PYTHON_VERSION="${PYTHON_VER:0:1}.${PYTHON_VER:1}"
            else
              PYTHON_VERSION="3.11"
            fi
            
            # Extract CUDA/ROCm versions
            if [[ "$DIR" =~ cuda([0-9]+) ]]; then
              CUDA_VER="${BASH_REMATCH[1]}"
              CUDA_VERSION="${CUDA_VER:0:2}.${CUDA_VER:2}"
              IMAGE_TAG="${CUDA_VERSION}"
            elif [[ "$DIR" =~ rocm([0-9]+) ]]; then
              ROCM_VER="${BASH_REMATCH[1]}"
              ROCM_VERSION="${ROCM_VER:0:1}.${ROCM_VER:1}"
              IMAGE_TAG="${ROCM_VERSION}"
            else
              IMAGE_TAG="latest"
            fi
            
            # Resolve variables
            RESOLVED_BASE_IMAGE="${RESOLVED_BASE_IMAGE//\$\{PYTHON_VERSION\}/$PYTHON_VERSION}"
            RESOLVED_BASE_IMAGE="${RESOLVED_BASE_IMAGE//\$\{IMAGE_TAG\}/$IMAGE_TAG}"
            
            echo "  Raw base: $BASE_IMAGE_RAW"
            echo "  Resolved: $RESOLVED_BASE_IMAGE"
            
            BASE_IMAGES["$RESOLVED_BASE_IMAGE"]=1
            DOCKERFILE_BASE_MAP["$DIR"]="$RESOLVED_BASE_IMAGE"
          done
          
          # Save discovery results
          for key in "${!DOCKERFILE_BASE_MAP[@]}"; do
            echo "$key=${DOCKERFILE_BASE_MAP[$key]}" >> results/dockerfile_base_map.txt
          done
          
          for key in "${!DOCKERFILE_CONTEXTS[@]}"; do
            echo "$key=${DOCKERFILE_CONTEXTS[$key]}" >> results/dockerfile_contexts.txt
          done
          
          echo "discovery_complete=true" >> $GITHUB_OUTPUT
          echo "base_image_count=${#BASE_IMAGES[@]}" >> $GITHUB_OUTPUT
          echo "dockerfile_count=${#TARGET_DIRS[@]}" >> $GITHUB_OUTPUT

      - name: Container Security Scan
        if: matrix.scan-type == 'container' && steps.discovery.outputs.discovery_complete == 'true'
        run: |
          echo "🐳 Starting comprehensive container security analysis..."
          
          # Load discovery results
          declare -A DOCKERFILE_BASE_MAP DOCKERFILE_CONTEXTS
          
          while IFS='=' read -r key value; do
            DOCKERFILE_BASE_MAP["$key"]="$value"
          done < results/dockerfile_base_map.txt
          
          while IFS='=' read -r key value; do
            DOCKERFILE_CONTEXTS["$key"]="$value"
          done < results/dockerfile_contexts.txt
          
          declare -A BASE_RESULTS CONTAINER_RESULTS
          
          # Comprehensive base image analysis
          echo "Analyzing base images with full Snyk container capabilities..."
          
          for base_image in $(cat results/dockerfile_base_map.txt | cut -d'=' -f2 | sort -u); do
            echo "🔍 Scanning base image: $base_image"
            base_key=$(echo "$base_image" | tr '/:' '_')
            
            # Full container scan with all options
            snyk container test "$base_image" \
              --json \
              --severity-threshold=low \
              --include-base-image-vulns \
              --exclude-base-image-vulns=false \
              > "results/container/base_${base_key}.json" 2>/dev/null || true
            
            # Monitor the image for ongoing tracking
            snyk container monitor "$base_image" \
              --project-name="base-image-${base_key}" \
              --org="${SNYK_ORG:-}" || true
            
            # Get license information
            snyk container test "$base_image" \
              --json \
              --print-deps \
              > "results/container/base_${base_key}_deps.json" 2>/dev/null || true
          done
          
          # Comprehensive Dockerfile analysis
          echo "Analyzing Dockerfiles with container context..."
          
          for dockerfile in "${!DOCKERFILE_CONTEXTS[@]}"; do
            dockerfile_path="${DOCKERFILE_CONTEXTS[$dockerfile]}"
            base_image="${DOCKERFILE_BASE_MAP[$dockerfile]}"
            
            echo "🔍 Scanning Dockerfile: $dockerfile"
            
            cd "$dockerfile_path"
            
            # Container scan with Dockerfile context
            snyk container test "$base_image" \
              --file=Dockerfile \
              --json \
              --severity-threshold=low \
              --include-base-image-vulns \
              > "../../../results/container/${dockerfile}_full.json" 2>/dev/null || true
            
            # Test with app dependencies if they exist
            if [ -f "requirements.txt" ]; then
              snyk container test "$base_image" \
                --file=Dockerfile \
                --app-vulns \
                --json \
                > "../../../results/container/${dockerfile}_app.json" 2>/dev/null || true
            fi
            
            # Monitor for ongoing tracking
            snyk container monitor "$base_image" \
              --file=Dockerfile \
              --project-name="dockerfile-${dockerfile}" \
              --org="${SNYK_ORG:-}" || true
            
            cd - > /dev/null
          done

      - name: Code Quality & Security Scan
        if: matrix.scan-type == 'code' && steps.discovery.outputs.discovery_complete == 'true'
        run: |
          echo "📝 Starting comprehensive code security analysis..."
          
          while IFS='=' read -r key value; do
            DOCKERFILE_CONTEXTS["$key"]="$value"
          done < results/dockerfile_contexts.txt
          
          # Snyk Code analysis for each Dockerfile context
          for dockerfile in "${!DOCKERFILE_CONTEXTS[@]}"; do
            dockerfile_path="${DOCKERFILE_CONTEXTS[$dockerfile]}"
            
            echo "🔍 Code scanning: $dockerfile"
            
            cd "$dockerfile_path"
            
            # Snyk Code for security issues in Dockerfiles and scripts
            snyk code test \
              --json \
              --severity-threshold=low \
              > "../../../results/code/${dockerfile}_code.json" 2>/dev/null || true
            
            # Monitor code for ongoing analysis
            snyk code monitor \
              --project-name="code-${dockerfile}" \
              --org="${SNYK_ORG:-}" || true
            
            cd - > /dev/null
          done

      - name: Infrastructure as Code Scan
        if: matrix.scan-type == 'iac' && steps.discovery.outputs.discovery_complete == 'true'
        run: |
          echo "🏗️ Starting Infrastructure as Code security analysis..."
          
          declare -A DOCKERFILE_CONTEXTS
          while IFS='=' read -r key value; do
            DOCKERFILE_CONTEXTS["$key"]="$value"
          done < results/dockerfile_contexts.txt
          
          # IaC analysis for Dockerfiles and related infrastructure files
          for dockerfile in "${!DOCKERFILE_CONTEXTS[@]}"; do
            dockerfile_path="${DOCKERFILE_CONTEXTS[$dockerfile]}"
            
            echo "🔍 IaC scanning: $dockerfile"
            
            cd "$dockerfile_path"
            
            # Snyk IaC for Dockerfile best practices and security configurations
            snyk iac test \
              --json \
              --severity-threshold=low \
              . > "../../../results/iac/${dockerfile}_iac.json" 2>/dev/null || true
            
            # Look for docker-compose files and scan them too
            if [ -f "docker-compose.yml" ] || [ -f "docker-compose.yaml" ]; then
              snyk iac test \
                --json \
                --severity-threshold=low \
                docker-compose.* > "../../../results/iac/${dockerfile}_compose.json" 2>/dev/null || true
            fi
            
            cd - > /dev/null
          done

      - name: Dependency Security Scan
        if: matrix.scan-type == 'dependencies' && steps.discovery.outputs.discovery_complete == 'true'
        run: |
          echo "📦 Starting comprehensive dependency security analysis..."
          
          declare -A DOCKERFILE_CONTEXTS
          while IFS='=' read -r key value; do
            DOCKERFILE_CONTEXTS["$key"]="$value"
          done < results/dockerfile_contexts.txt
          
          # Dependency analysis for each context
          for dockerfile in "${!DOCKERFILE_CONTEXTS[@]}"; do
            dockerfile_path="${DOCKERFILE_CONTEXTS[$dockerfile]}"
            
            echo "🔍 Dependency scanning: $dockerfile"
            
            cd "$dockerfile_path"
            
            # Python dependencies
            if [ -f "requirements.txt" ]; then
              echo "  Scanning requirements.txt"
              snyk test \
                --file=requirements.txt \
                --json \
                --severity-threshold=low \
                > "../../../results/dependencies/${dockerfile}_requirements.json" 2>/dev/null || true
              
              snyk monitor \
                --file=requirements.txt \
                --project-name="deps-${dockerfile}-requirements" \
                --org="${SNYK_ORG:-}" || true
            fi
            
            # Pipfile dependencies
            if [ -f "Pipfile" ]; then
              echo "  Scanning Pipfile"
              snyk test \
                --file=Pipfile \
                --json \
                --severity-threshold=low \
                > "../../../results/dependencies/${dockerfile}_pipfile.json" 2>/dev/null || true
            fi
            
            # Node.js dependencies
            if [ -f "package.json" ]; then
              echo "  Scanning package.json"
              snyk test \
                --file=package.json \
                --json \
                --severity-threshold=low \
                > "../../../results/dependencies/${dockerfile}_npm.json" 2>/dev/null || true
            fi
            
            # Generic dependency scanning
            snyk test \
              --json \
              --all-projects \
              --severity-threshold=low \
              . > "../../../results/dependencies/${dockerfile}_all.json" 2>/dev/null || true
            
            cd - > /dev/null
          done

  comprehensive-report:
    name: Generate Comprehensive Security Report
    runs-on: ubuntu-latest
    needs: snyk-comprehensive-scan
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all scan results
        uses: actions/download-artifact@v4
        with:
          pattern: snyk-*
          merge-multiple: true
          path: results/

      - name: Generate Comprehensive Report
        run: |
          echo "📊 Generating comprehensive security report..."
          
          # Ensure jq is available
          sudo apt-get update && sudo apt-get install -y jq
          
          {
            echo "# 🛡️ Comprehensive Snyk Security Analysis Report"
            echo ""
            echo "## 📋 Executive Summary"
            echo ""
            
            # Count total issues across all scan types
            TOTAL_CRITICAL=0
            TOTAL_HIGH=0
            TOTAL_MEDIUM=0
            TOTAL_LOW=0
            
            # Process all JSON results
            for json_file in $(find results -name "*.json" -type f); do
              if jq -e . "$json_file" >/dev/null 2>&1; then
                CRITICAL=$(jq '[.vulnerabilities[]? | select(.severity == "critical")] | length' "$json_file" 2>/dev/null || echo "0")
                HIGH=$(jq '[.vulnerabilities[]? | select(.severity == "high")] | length' "$json_file" 2>/dev/null || echo "0")
                MEDIUM=$(jq '[.vulnerabilities[]? | select(.severity == "medium")] | length' "$json_file" 2>/dev/null || echo "0")
                LOW=$(jq '[.vulnerabilities[]? | select(.severity == "low")] | length' "$json_file" 2>/dev/null || echo "0")
                
                TOTAL_CRITICAL=$((TOTAL_CRITICAL + CRITICAL))
                TOTAL_HIGH=$((TOTAL_HIGH + HIGH))
                TOTAL_MEDIUM=$((TOTAL_MEDIUM + MEDIUM))
                TOTAL_LOW=$((TOTAL_LOW + LOW))
              fi
            done
            
            echo "| Severity | Count |"
            echo "|:---------|:------|"
            echo "| 🔴 Critical | $TOTAL_CRITICAL |"
            echo "| 🟠 High | $TOTAL_HIGH |"
            echo "| 🟡 Medium | $TOTAL_MEDIUM |"
            echo "| 🔵 Low | $TOTAL_LOW |"
            echo "| **Total** | **$((TOTAL_CRITICAL + TOTAL_HIGH + TOTAL_MEDIUM + TOTAL_LOW))** |"
            echo ""
            
            # Overall risk assessment
            if [ $TOTAL_CRITICAL -gt 0 ]; then
              echo "🚨 **Risk Level: CRITICAL** - Immediate action required"
            elif [ $TOTAL_HIGH -gt 0 ]; then
              echo "⚠️ **Risk Level: HIGH** - Priority remediation needed"
            elif [ $TOTAL_MEDIUM -gt 0 ]; then
              echo "🟡 **Risk Level: MEDIUM** - Moderate risk"
            else
              echo "✅ **Risk Level: LOW** - Good security posture"
            fi
            echo ""
            
            # Container Security Section
            echo "## 🐳 Container Security Analysis"
            echo ""
            
            if ls results/container/*.json >/dev/null 2>&1; then
              echo "### Base Images"
              echo ""
              echo "| Image | Critical | High | Medium | Low | Status |"
              echo "|:------|:---------|:-----|:-------|:----|:-------|"
              
              for container_file in results/container/base_*.json; do
                if [ -f "$container_file" ] && jq -e . "$container_file" >/dev/null 2>&1; then
                  image_name=$(basename "$container_file" .json | sed 's/base_//' | tr '_' '/')
                  crit=$(jq '[.vulnerabilities[]? | select(.severity == "critical")] | length' "$container_file")
                  high=$(jq '[.vulnerabilities[]? | select(.severity == "high")] | length' "$container_file")
                  med=$(jq '[.vulnerabilities[]? | select(.severity == "medium")] | length' "$container_file")
                  low=$(jq '[.vulnerabilities[]? | select(.severity == "low")] | length' "$container_file")
                  
                  if [ $crit -gt 0 ]; then
                    status="🔴 Critical"
                  elif [ $high -gt 0 ]; then
                    status="🟠 High Risk"
                  elif [ $med -gt 0 ]; then
                    status="🟡 Medium Risk"
                  else
                    status="✅ Clean"
                  fi
                  
                  echo "| \`$image_name\` | $crit | $high | $med | $low | $status |"
                fi
              done
              echo ""
              
              echo "### Dockerfile Analysis"
              echo ""
              echo "| Dockerfile | Critical | High | Medium | Low | Status |"
              echo "|:-----------|:---------|:-----|:-------|:----|:-------|"
              
              for dockerfile_file in results/container/*_full.json; do
                if [ -f "$dockerfile_file" ] && jq -e . "$dockerfile_file" >/dev/null 2>&1; then
                  dockerfile_name=$(basename "$dockerfile_file" _full.json)
                  crit=$(jq '[.vulnerabilities[]? | select(.severity == "critical")] | length' "$dockerfile_file")
                  high=$(jq '[.vulnerabilities[]? | select(.severity == "high")] | length' "$dockerfile_file")
                  med=$(jq '[.vulnerabilities[]? | select(.severity == "medium")] | length' "$dockerfile_file")
                  low=$(jq '[.vulnerabilities[]? | select(.severity == "low")] | length' "$dockerfile_file")
                  
                  if [ $crit -gt 0 ]; then
                    status="🔴 Critical"
                  elif [ $high -gt 0 ]; then
                    status="🟠 High Risk"
                  else
                    status="✅ Acceptable"
                  fi
                  
                  echo "| \`$dockerfile_name\` | $crit | $high | $med | $low | $status |"
                fi
              done
              echo ""
            fi
            
            # Code Security Section
            echo "## 📝 Code Security Analysis"
            echo ""
            
            if ls results/code/*.json >/dev/null 2>&1; then
              echo "| Component | Issues | Severity |"
              echo "|:----------|:-------|:---------|"
              
              for code_file in results/code/*.json; do
                if [ -f "$code_file" ] && jq -e . "$code_file" >/dev/null 2>&1; then
                  component=$(basename "$code_file" _code.json)
                  issues=$(jq '.runs[0].results | length' "$code_file" 2>/dev/null || echo "0")
                  high_issues=$(jq '[.runs[0].results[]? | select(.level == "high")] | length' "$code_file" 2>/dev/null || echo "0")
                  
                  if [ $high_issues -gt 0 ]; then
                    severity="🔴 High"
                  elif [ $issues -gt 0 ]; then
                    severity="🟡 Medium"
                  else
                    severity="✅ Clean"
                  fi
                  
                  echo "| \`$component\` | $issues | $severity |"
                fi
              done
              echo ""
            fi
            
            # Infrastructure as Code Section
            echo "## 🏗️ Infrastructure as Code Analysis"
            echo ""
            
            if ls results/iac/*.json >/dev/null 2>&1; then
              echo "| Component | Issues | Critical | High | Medium |"
              echo "|:----------|:-------|:---------|:-----|:-------|"
              
              for iac_file in results/iac/*.json; do
                if [ -f "$iac_file" ] && jq -e . "$iac_file" >/dev/null 2>&1; then
                  component=$(basename "$iac_file" .json | sed 's/_iac$//' | sed 's/_compose$//')
                  total=$(jq '.infrastructureAsCodeIssues | length' "$iac_file" 2>/dev/null || echo "0")
                  crit=$(jq '[.infrastructureAsCodeIssues[]? | select(.severity == "critical")] | length' "$iac_file" 2>/dev/null || echo "0")
                  high=$(jq '[.infrastructureAsCodeIssues[]? | select(.severity == "high")] | length' "$iac_file" 2>/dev/null || echo "0")
                  med=$(jq '[.infrastructureAsCodeIssues[]? | select(.severity == "medium")] | length' "$iac_file" 2>/dev/null || echo "0")
                  
                  echo "| \`$component\` | $total | $crit | $high | $med |"
                fi
              done
              echo ""
            fi
            
            # Dependencies Section
            echo "## 📦 Dependency Security Analysis"
            echo ""
            
            if ls results/dependencies/*.json >/dev/null 2>&1; then
              echo "| Component | Type | Vulnerabilities | Licenses | Status |"
              echo "|:----------|:-----|:----------------|:---------|:-------|"
              
              for dep_file in results/dependencies/*.json; do
                if [ -f "$dep_file" ] && jq -e . "$dep_file" >/dev/null 2>&1; then
                  component=$(basename "$dep_file" .json | cut -d'_' -f1)
                  dep_type=$(basename "$dep_file" .json | cut -d'_' -f2)
                  vulns=$(jq '.vulnerabilities | length' "$dep_file" 2>/dev/null || echo "0")
                  licenses=$(jq '.licenseIssues | length' "$dep_file" 2>/dev/null || echo "0")
                  high_vulns=$(jq '[.vulnerabilities[]? | select(.severity == "high" or .severity == "critical")] | length' "$dep_file" 2>/dev/null || echo "0")
                  
                  if [ $high_vulns -gt 0 ]; then
                    status="🔴 High Risk"
                  elif [ $vulns -gt 0 ]; then
                    status="🟡 Medium Risk"
                  else
                    status="✅ Clean"
                  fi
                  
                  echo "| \`$component\` | $dep_type | $vulns | $licenses | $status |"
                fi
              done
              echo ""
            fi
            
            # Recommendations Section
            echo "## 🔧 Remediation Recommendations"
            echo ""
            echo "### Immediate Actions Required"
            echo ""
            
            if [ $TOTAL_CRITICAL -gt 0 ]; then
              echo "🚨 **Critical vulnerabilities detected** - Address immediately:"
              echo "1. Review and update base images to latest secure versions"
              echo "2. Update vulnerable dependencies to patched versions"
              echo "3. Apply security patches for critical issues"
              echo ""
            fi
            
            if [ $TOTAL_HIGH -gt 0 ]; then
              echo "⚠️ **High-priority vulnerabilities** - Address within 7 days:"
              echo "1. Audit high-severity findings"
              echo "2. Implement security best practices"
              echo "3. Consider alternative packages if patches unavailable"
              echo ""
            fi
            
            echo "### Long-term Security Improvements"
            echo ""
            echo "1. **Implement continuous monitoring** with Snyk integration"
            echo "2. **Establish security gates** in CI/CD pipeline"
            echo "3. **Regular security training** for development team"
            echo "4. **Automated dependency updates** with security focus"
            echo "5. **Container image scanning** in registry"
            echo ""
            
            echo "---"
            echo "*Report generated at $(date -u '+%Y-%m-%d %H:%M UTC') | Powered by Snyk Comprehensive Analysis*"
            
          } >> $GITHUB_STEP_SUMMARY

      - name: Upload Comprehensive Results
        uses: actions/upload-artifact@v4
        with:
          name: snyk-comprehensive-results-${{ github.run_number }}
          path: results/
          retention-days: 30

      - name: Create SARIF Report
        if: always()
        run: |
          echo "Converting results to SARIF format for GitHub Security tab..."
          
          # Convert Snyk results to SARIF format
          find results -name "*.json" -type f | while read -r json_file; do
            if jq -e . "$json_file" >/dev/null 2>&1; then
              snyk-to-html -i "$json_file" -o "${json_file%.json}.html" || true
            fi
          done

      - name: Upload to GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results/
          category: snyk-comprehensive-scan
        continue-on-error: true

      - name: Notify on Critical Issues
        if: failure()
        uses: 8398a7/action-slack@v3
        with:
          status: failure
          text: |
            🚨 Critical security vulnerabilities detected in comprehensive scan
            Critical: ${{ env.TOTAL_CRITICAL || 0 }}
            High: ${{ env.TOTAL_HIGH || 0 }}
            View details: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}