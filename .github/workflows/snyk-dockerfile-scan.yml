name: Container Security Scan

on:
  push:
    branches: ["main"]
  schedule:
    - cron: "0 0 * * *"
  pull_request:
    branches: ["main"]
    types: [opened, synchronize, reopened, labeled]

jobs:
  snyk-container-scan:
    if: ${{ github.event_name != 'pull_request' || contains(github.event.pull_request.labels.*.name, 'security-scan') }}
    runs-on: ubuntu-latest
    concurrency:
      group: snyk-container-scan
      cancel-in-progress: true

    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Snyk CLI
        uses: snyk/actions/setup@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Scan Dockerfiles with Snyk
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: |
          set -u

          mapfile -t DOCKERFILES < <(find . -type f -iname "Dockerfile*" -not -path "*/.*")
          [ ${#DOCKERFILES[@]} -eq 0 ] && { echo "No Dockerfiles found."; exit 0; }

          SUMMARY=""
          TOTAL_CRITICAL=0
          TOTAL_HIGH=0
          TOTAL_MEDIUM=0
          TOTAL_LOW=0
          SCAN_FAILURE=0

          for FILE in "${DOCKERFILES[@]}"; do
            TEMP_DOCKERFILE="temp-$(basename "${FILE}").Dockerfile"
            cp "${FILE}" "${TEMP_DOCKERFILE}"

            FINAL_FROM=$(grep -Ei '^FROM ' "${FILE}" | tail -1)
            VARS=$(echo "$FINAL_FROM" | grep -oE '\$\{?[A-Za-z_][A-Za-z0-9_]*\}?' || true)

            if [ -n "$VARS" ]; then
              grep '^ARG ' "${FILE}" | while read -r argline; do
                VAR_NAME=$(echo "$argline" | cut -d' ' -f2 | cut -d= -f1)
                DEFAULT_VAL=$(echo "$argline" | grep '=' | cut -d= -f2- | tr -d '"'"'" || true)
                [ -n "$DEFAULT_VAL" ] && export "${VAR_NAME}"="${DEFAULT_VAL}"
              done

              envsubst "$(echo "$VARS" | tr ' ' '\n' | sed 's/[\$\{\}]//g' | xargs)" \
                < "${TEMP_DOCKERFILE}" > "${TEMP_DOCKERFILE}.resolved"
              mv "${TEMP_DOCKERFILE}.resolved" "${TEMP_DOCKERFILE}"
            fi

            FINAL_FROM_RESOLVED=$(grep -Ei '^FROM ' "${TEMP_DOCKERFILE}" | tail -1)
            if echo "$FINAL_FROM_RESOLVED" | grep -q '\$\|{{\|}}'; then
              sed -i 's|^FROM.*|FROM registry.access.redhat.com/ubi8/ubi:latest|' "${TEMP_DOCKERFILE}"
              BASE_IMAGE_NAME="registry.access.redhat.com/ubi8/ubi:latest (fallback)"
            else
              BASE_IMAGE_NAME=$(echo "$FINAL_FROM_RESOLVED" | sed -E 's/^FROM[[:space:]]+//I')
            fi

            snyk container test "${BASE_IMAGE_NAME}" --file="${TEMP_DOCKERFILE}" --exclude-app-vulns --json \
              > "snyk-result.json" 2> "snyk-error.log" || echo '{"vulnerabilities":[]}' > "snyk-result.json"

            if jq -e 'has("vulnerabilities")' snyk-result.json &>/dev/null; then
              CRIT=$(jq -r '.vulnerabilities | if . == null then 0 else map(select(.severity=="critical")) | length end' snyk-result.json)
              HIGH=$(jq -r '.vulnerabilities | if . == null then 0 else map(select(.severity=="high")) | length end' snyk-result.json)
              MED=$(jq -r '.vulnerabilities | if . == null then 0 else map(select(.severity=="medium")) | length end' snyk-result.json)
              LOW=$(jq -r '.vulnerabilities | if . == null then 0 else map(select(.severity=="low")) | length end' snyk-result.json)

              SUMMARY="$SUMMARY- $FILE: ${CRIT}C ${HIGH}H ${MED}M ${LOW}L (base: $BASE_IMAGE_NAME)"$'\n'
              TOTAL_CRITICAL=$((TOTAL_CRITICAL + CRIT))
              TOTAL_HIGH=$((TOTAL_HIGH + HIGH))
              TOTAL_MEDIUM=$((TOTAL_MEDIUM + MED))
              TOTAL_LOW=$((TOTAL_LOW + LOW))
            else
              ERROR_MSG=$(head -n1 snyk-error.log || echo "Unknown error")
              SUMMARY="$SUMMARY- $FILE: Scan failed - $ERROR_MSG"$'\n'
              SCAN_FAILURE=1
            fi

            rm -f snyk-result.json snyk-error.log "${TEMP_DOCKERFILE}"
          done

          echo "SUMMARY<<EOF" >> $GITHUB_ENV
          echo "Dockerfiles scanned: ${#DOCKERFILES[@]}" >> $GITHUB_ENV
          echo "Critical: ${TOTAL_CRITICAL}, High: ${TOTAL_HIGH}, Medium: ${TOTAL_MEDIUM}, Low: ${TOTAL_LOW}" >> $GITHUB_ENV
          echo "" >> $GITHUB_ENV
          echo "Details:" >> $GITHUB_ENV
          echo "$SUMMARY" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          echo "SCAN_FAILURE=${SCAN_FAILURE}" >> $GITHUB_ENV

      - name: Slack Notification
        if: ${{ always() }}
        env:
          SUMMARY: ${{ env.SUMMARY }}
          SCAN_FAILURE: ${{ env.SCAN_FAILURE }}
        run: |
          [ -z "${SLACK_WEBHOOK_URL}" ] && { echo "No Slack webhook URL. Skipping notification."; exit 0; }

          STATUS=":white_check_mark: *Scan successful*"
          [ "$SCAN_FAILURE" -ne 0 ] && STATUS=":x: *Scan completed with errors*"

          PAYLOAD="{\"text\":\"${STATUS}\n${SUMMARY}\"}"
          curl -X POST -H 'Content-type: application/json' --data "$PAYLOAD" "$SLACK_WEBHOOK_URL"
